.panel.panel-primary
    .panel-heading.text-center New courses
    .panel-body
        .row(ng-repeat='course in courses | filter : { featured: false}')
            .col-md-4 {{course.published | date:'MMM: d'}}
            .col-md-8 {{course.name}}